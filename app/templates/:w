{% extends "base.html" %}

{% block title %}freets - {{ location.name }}{% endblock %}

{% block banner_content %}

<h4 class="white-text">Free food at {{ location.name }}</h4>
<h5 class="white-text">{{ location.num_events() }}</h5>

{% endblock %}

{% block page_content %}


<div class="locations panel panel-default">
  <ul class="list-group">

    <!-- List events at this location -->
    {% include 'eventlist.html' %}

  </ul>
</div>
{% endblock %}
